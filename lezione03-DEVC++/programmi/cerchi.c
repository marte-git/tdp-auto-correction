/* programma che chiede e legge da input i raggi di tre circonferenze   e produce per ciascuna circonferenza la relativa area */#include <stdio.h>#define PI 3.14159int main () {  double primoRaggio, secondoRaggio, terzoRaggio; /* i tre raggi */  printf ("Gentile utente, forniscimi il raggio della prima circonferenza, please: ");    scanf("%lf", &primoRaggio);  printf ("Mi* car* utente, ma l'area della circonferenza che ha raggio %g \n", primoRaggio);  printf ("altro non puo' essere che %g!\n", primoRaggio*primoRaggio*PI);  printf ("\nOra, se non ti spiace, forniscimi il raggio della seconda circonferenza, please: ");    scanf("%lf", &secondoRaggio);  printf ("Mi* car* utente, ma l'area della circonferenza che ha raggio %g \n", secondoRaggio);  printf ("altro non puo' essere che %g!\n", secondoRaggio*secondoRaggio*PI);  printf ("Ed infine, se non hai da fare, forniscimi il raggio della seconda circonferenza, please, che cosi' la finiamo una buona volta: ");    scanf("%lf", &terzoRaggio);  printf ("Mi* car* utente, ma l'area della circonferenza che ha raggio %g \n", terzoRaggio);  printf ("altro non puo' essere che %g!\n", terzoRaggio*terzoRaggio*PI);printf ("\n\nFine programma: cari saluti.\n");return 0;}/* la stampa per il terzo calcolo andrebbe distanziata da quella per il secondo ... come fare? */