/* questo programma, ricevendo in INPUT i lati significativi di un rettangolo,    ne calcola e stampa in OUTPUT l'area    */#include <stdio.h> int main () {  int primoLato, secondoLato,		/* i lati da ricevere via INPUT */      area;							/* risultato ... *//* lettura dei dati */  printf (" Oh utente, forniscimi gentilmente i lati, che cosi' ci lavoro:\n ");    printf (" ... dunque, dammi il primo lato: ");    scanf("%d", &primoLato);    printf (" grazie mille; ora dammi il secondo lato pls.: ");    scanf("%d", &secondoLato);    printf ("Benissimo, cosi' per informazione ti dico che ho letto i seguenti lati: %d, %d\n\n", primoLato, secondoLato);  /* calcolo dell'area */  area = primoLato*secondoLato;         /* calcolo */  printf (" Stimato user, il rettangolo di lati %d e %d ha area %d\n", primoLato, secondoLato, area);printf ("\nFINE");return 0;}